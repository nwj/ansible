---
- name: restart nginx
  service:
    name: nginx
    state: restarted

- name: restart postfix
  service:
    name: postfix
    state: restarted

- name: export huginn service
  shell: "bundle exec rake production:export"
  args:
    chdir: "{{ huginn_install_path }}"
  become: yes
